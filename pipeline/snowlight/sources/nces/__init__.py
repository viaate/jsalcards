"""National Center for Education Statistics (NCES) source files.

* :mod:`snowlight.sources.nces.fetch` downloads files with caching and SHA-256
  provenance.
* :mod:`snowlight.sources.nces.readers` parses the EDGE geocode, CCD and PSS files
  into typed polars frames, keeping every value exactly as published.
"""

"""Tests for the published file contract (``snowlight.schemas``).

This file makes the directory a package, so mypy names its modules
``schemas.synthetic`` and so on rather than clashing with other test folders.
"""
